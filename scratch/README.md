# Scratch workspace

Disposable retrieval, capture, conversion, and extraction files may be placed
here. All contents other than this policy file are ignored by Git.

Scratch contents must never be the sole durable locator for a cited evidence
record. Anything that must survive the session belongs in the session package
under `sessions/`, in `sources/metadata/`, or in `sources/public/`.
